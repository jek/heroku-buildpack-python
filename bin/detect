#!/bin/sh

# This detector is an interceptor, aborting detection if <build-dir>  
# holds a cached build result.

BUILD_DIR=$1

# Exit early if app is already built
if [ -f "$BUILD_DIR/__cached_slug__" ]; then
  exit 1
fi

# Continue to our regularly scheduled detector
exec bin/detect.stock
